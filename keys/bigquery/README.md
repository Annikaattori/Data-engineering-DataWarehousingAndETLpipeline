# BigQuery API key

Place your BigQuery API key or service account JSON file in this directory. The
pipeline defaults to looking for a file named `api_key.json` in this folder, but
you can override the path with the `BIGQUERY_API_KEY_PATH` environment variable
if you keep the key elsewhere.
